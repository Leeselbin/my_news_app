# =================================================================
# 기본 시스템 파일
# =================================================================
.DS_Store
Thumbs.db

# =================================================================
# 의존성 (Dependencies)
# =================================================================
# npm, yarn, pnpm 등으로 설치되는 패키지 폴더
node_modules/

# =================================================================
# 빌드 산출물 (Build outputs)
# =================================================================
# create-react-app의 기본 빌드 폴더
build/
# netlify, vercel 등 다른 빌드 도구의 산출물 폴더
.netlify/
.vercel/
dist/

# =================================================================
# 로그 파일 (Logs)
# =================================================================
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# =================================================================
# 환경 변수 파일 (Environment variables)
# =================================================================
# 민감한 정보(API 키 등)를 담고 있으므로 절대 Git에 올리면 안 됩니다.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# =================================================================
# 테스트 및 커버리지 리포트 (Test & Coverage reports)
# =================================================================
coverage/
.jest-cache

# =================================================================
# 에디터 및 IDE 설정 파일
# =================================================================
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?